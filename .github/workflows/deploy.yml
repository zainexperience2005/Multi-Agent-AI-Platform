name: Build and Push to AWS ECR

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-push:
    name: Build & Push Services
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Log in to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # 1. Auth Service
      - name: Build and Push Auth Service
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/services/auth/Dockerfile
          push: true
          tags: |
            ${{ steps.login-ecr.outputs.registry }}/nexus-auth:latest
            ${{ steps.login-ecr.outputs.registry }}/nexus-auth:${{ github.sha }}

      # # 2. Chat Service
      # - name: Build and Push Chat Service
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: ./backend
      #     file: ./backend/services/chat/Dockerfile
      #     push: true
      #     tags: |
      #       ${{ steps.login-ecr.outputs.registry }}/nexus-chat:latest
      #       ${{ steps.login-ecr.outputs.registry }}/nexus-chat:${{ github.sha }}

      # # 3. Agent Service
      # - name: Build and Push Agent Service
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: ./backend
      #     file: ./backend/services/agent/Dockerfile
      #     push: true
      #     tags: |
      #       ${{ steps.login-ecr.outputs.registry }}/nexus-agent:latest
      #       ${{ steps.login-ecr.outputs.registry }}/nexus-agent:${{ github.sha }}

      # # 4. Gateway Service
      # - name: Build and Push Gateway
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: ./backend
      #     file: ./backend/gateway/Dockerfile
      #     push: true
      #     tags: |
      #       ${{ steps.login-ecr.outputs.registry }}/nexus-gateway:latest
      #       ${{ steps.login-ecr.outputs.registry }}/nexus-gateway:${{ github.sha }}
